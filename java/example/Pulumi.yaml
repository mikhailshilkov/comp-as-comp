name: java-example-yaml
runtime: yaml
packages:
  java-components: ..
resources:
  myrandom:
    type: java-components:StaticPage
    properties:
      indexContent: "Hello, World!"
outputs:
  endpoint: ${myrandom.endpoint}
